# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Optional: model for coaching + document extraction (default: gemini-2.5-flash).
# If you get 404 on the model, try: COACHING_MODEL=gemini-2.0-flash-001
# COACHING_MODEL=gemini-2.5-flash

# Server Configuration
PORT=8080
